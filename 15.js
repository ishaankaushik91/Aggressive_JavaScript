// Joining all elements of an Array (LOOP)
let  myColor = ["Red", "Green", "White", "Black"];

let New_stuff = "";
for (let i = 0; i < myColor.length; i++)
{
    New_stuff += myColor[i]
}
console.log(New_stuff);